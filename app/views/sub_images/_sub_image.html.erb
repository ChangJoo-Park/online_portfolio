<% domid = "sub_#{sub_image.id}" %>
<%= image_tag sub_image.image.url(:thumb), class: "thumbnail" %>
<% sub_image.caption %>
<% link_to "", [sub_image.project, sub_image], method: :delete,remote: true, data: { confirm:'Are you sure? '}, class: "glyphicon glyphicon-trash" %>