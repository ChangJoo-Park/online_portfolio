<div class="container">
<h1>Listing Projects <span><%= link_to 'New Project', "#new_project", data: { toggle: "modal" }, class: "btn btn-primary" %></span></h1>
<%= render "modal_new" %>
<hr>
	<div class="row">
		<%= render 'list' %>
	</div>
</div>
<%= debug @project %>